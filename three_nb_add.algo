var str = "I am taking class at Gomycode.";
var wordCount = str.match(/(\w+)/g).length;
alert(wordCount); 



const vowelCount = str => {
  let vowels = /[aeiou]/gi;
  let result = str.match(vowels);
  let count = result.length;

  console.log(count);
};